<h2>Edit the entry for <%= @album.title %></h2>

<%= render 'shared/errors' %>

<%= render 'form', bands: @bands, album_artist: @album_artist, album: @album %>

<%= button_to 'Cancel', album_url(@album), method: :get %>
